/*this file is from AI*/
/*could not include comments in package.json/package-lock.json, so I included them here. This section/set of lines of code within those files are from AI: "scripts": {
        "e2e": "playwright test",
        "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --config jest.config.js"
    },
    "devDependencies": {
        "@playwright/test": "^1.60.0",
        "jest": "^30.4.2",
        "supertest": "^7.2.2"
    },*/
import dotenv from "dotenv";

dotenv.config({ path: "../backend/.env" });