___________________________________________________Larry Zhou__________________________________________________

Prompt1: can u edit the CSS so that the post button is at the bottom of the screen and the friend username search 
bar is on the right side. make it like 40 pixels wide.

Done! I've made the CSS updates:

Post button at the bottom: Changed .logged-in-container to use justify-content: flex-end so the post form moves to the bottom of the 
screen
Friend search bar on the right: Changed .friend_req_bar to position fixed on the right side (40px from the edge) with horizontal layout, 
and set the input width to 40px
The friend username search input is now a compact 40px-wide field positioned on the right side, and the post form will appear 
at the bottom of the screen.

Reflection: This was definately a change that I could have done myself because the postitioning of objects in the CSS file doesn't seem
too hard, but it is pretty convenient that I can ask AI to format things for me.



Prompt 1: can u make it so that for posts the times are all saved in military time?


Response: I'll help you update the codebase to use military time (24-hour format) for posts and the old post clearing
feature. Let me first explore the relevant files to understand the current implementation.

First, I'll create a utility function for military time formatting on the backend
(generates timeUtils.js and calls the functions in post.js)
Done! I've implemented military time (24-hour format) throughout your codebase:
Backend
Created timeUtils.js - Utility functions for military time handling
//This file was generated by AI. refer to prompt 1 in the get_AI.txt for more details.

/**
 * Utility functions for handling military time (24-hour format)
 * All times should be stored and processed in military time format (HH:mm)
 */

/**
 * Converts a time string to military format (24-hour HH:mm format)
 * @param {string} timeStr - Time string in any common format
 * @returns {string} Time in military format (HH:mm)
 */
export const toMilitaryTime = (timeStr) => {
  if (!timeStr) return '';
  // If already in military format (HH:mm), return as-is
  if (/^\d{2}:\d{2}$/.test(timeStr)) {
    return timeStr;
  }
  // Add more format conversions as needed
  return timeStr;
};

/**
 * Validates if a time string is in military format
 * @param {string} timeStr - Time string to validate
 * @returns {boolean} True if in military format (HH:mm)
 */
export const isMilitaryTime = (timeStr) => {
  return /^([0-1][0-9]|2[0-3]):[0-5][0-9]$/.test(timeStr);
};

/**
 * Creates a Date object from date and military time strings
 * @param {string} dateStr - Date in YYYY-MM-DD format
 * @param {string} militaryTimeStr - Time in military format HH:mm
 * @returns {Date} Combined date and time
 */
export const createDateFromMilitaryTime = (dateStr, militaryTimeStr) => {
  return new Date(`${dateStr} ${militaryTimeStr}`);
};

/**
 * Validates end time is after start time (both in military format)
 * @param {string} startTime - Start time in military format (HH:mm)
 * @param {string} endTime - End time in military format (HH:mm)
 * @returns {boolean} True if endTime is after startTime
 */
export const isValidTimeRange = (startTime, endTime) => {
  return endTime > startTime;
};

export default {
  toMilitaryTime,
  isMilitaryTime,
  createDateFromMilitaryTime,
  isValidTimeRange
};


Updated post.js:       const postDateTime = createDateFromMilitaryTime(post.date, post.endTime);  //This line was written by AI.

Added military time utility imports
Added clarifying comments (e.g., "14:30" = 2:30 PM)


Reflection: I used AI here because I thought to get the current time there was some special library I had to import.
It turns out there wasn't so I just went in and looked at the logic of the timeUtils.js file. 
Then I went onto the website and tested a bunch of dates after 11:59AM, and then went onto the database to see if it 
stored correctly. It did.




Prompt 2: can u just make it stored as military time, but when displaying it in the feed it is still regular time

Response: I'll create a utility function to convert military time to 12-hour format and update the frontend display components 
to use it.

Done! Now times are stored in military format (24-hour) but displayed in regular 12-hour format (with AM/PM) in the feeds.

Created timeUtils.js - Conversion function to display military time as 12-hour format (e.g., "14:30" → "2:30 PM")
//This file was generated by AI. refer to prompt 2 in the get_AI.txt for more details.

/**
 * Converts military time (24-hour format) to 12-hour format with AM/PM
 * @param {string} militaryTime - Time in military format (HH:mm, e.g., "14:30")
 * @returns {string} Time in 12-hour format (e.g., "2:30 PM")
 */
export const convertToTwelveHour = (militaryTime) => {
  if (!militaryTime) return '';
  
  const [hours, minutes] = militaryTime.split(':');
  const hour = parseInt(hours);
  const min = minutes;
  
  const period = hour >= 12 ? 'PM' : 'AM';
  const displayHour = hour % 12 || 12; // Convert 0 to 12 for midnight, keep others as-is
  
  return `${displayHour}:${min} ${period}`;
};

export default {
  convertToTwelveHour
};



Updated FriendsFeed.jsx - Uses convertToTwelveHour() to display times as 12-hour
Updated history.jsx - Uses convertToTwelveHour() for 12-hour display
Updated statistics.jsx - Uses convertToTwelveHour() for 12-hour display

Reflection: I looked at the code and compared it to the backend timeUtils.js. The on in the frontend reversed it back from 
military time correctly. I thought I was going to have to actually imcorporate the functions into my other jsx files but it did them for me.
I went back onto the website and went on a friended account to see if the post display was correct. It was. 
I did not end up adopting the changes in statstics.jsx




Prompt 3: can u make all the post related features a solid background color? like white? with 75% opacity?

Response: I'll add white backgrounds with 75% opacity to all post-related elements. Let me update the CSS:

Done! I've updated all post-related elements to have a solid white background with 75% opacity:

Updated elements:

.feed-post - Individual posts in the feed
.post - Post items in history/stats views
.post_info - Post container for history/stats
.post-form - Post creation form
.emoji-btn - Emoji reaction buttons
.reaction-chip - Reaction summary chips
.comment-input - Comment input field
.form-group input - Form inputs in the post form
All post-related features now have a consistent white background with 75% opacity, which provides good visibility while showing the background image through the semi-transparent overlay.

Reflection:
I went over the CSS changes it made and it looked relatively correct, but I wasn't sure because we never went over CSS in the class.
However I went on the website and the words were a little hard to read because the opacity was too low.




Prompt 4: make it more like 85% opacity

response:
Done! Updated all post-related elements to 85% opacity for better visibility of the background image.

Reflection: I made sure I turned all the 75's in the CSS file to 85s, and then went onto the website to check again. The opacity was the way I liked it to be.


can u just make the usernames on the post bigger

prompt 5: can u just make the usernames on the post bigger?

Response: Done! Increased the username font size from 12px to 16px.

Reflection: I was satisfied with the new font size for the username on the post section. 



Prompt 5: the study stats log seems a bit bland. can u try and embelish it a bit?

Response: Done! I've embellished the stats display with several improvements:

Enhanced Features:

Card-based design with gradient backgrounds and border accents
Icon emojis (📚 for sessions, ⏱️ for study time) for visual interest
Better typography with clear labels and large, bold numbers
Visual progress bar showing study streak
Hover effects - cards lift up and glow on hover
Improved spacing with better padding and gaps
Color accents using the app's accent color scheme
The stats now have a modern, polished look that matches the overall design aesthetic. The cards are interactive with smooth transitions, 
and the information is presented in a much more engaging way!

Relection: To be honest, I definately could have been more specific with the prompt. However I didn't even know what I was looking to make, since
my creativity in design is lacking. 


prompt 6: Can u add that purple border for the post UI and post history UI as well?
Done! I've added the purple border accent to all post-related UI elements:

The changes are all made in App.css and Index.css
Updated Elements:

.post-form - Create post form
.feed-post - Posts in the friends feed
.post - Posts in history/stats views
All now have the 2px solid rgba(170, 59, 255, 0.3) purple border that matches the stats cards for a cohesive design.

Reflection: At this point because I don't know CSS I have just gone wild with AI and CSS. I really liked the purple border since its my favorite color.
I kept this change.


prompt 7: can u embelish the fonts for these too? And try some creative styling on the buttons and input bars

Done! I've significantly embellished the styling with:

Major Enhancements:

Dramatic Multi-Stop Gradients on all cards with subtle color shifts
Layered Shadows creating depth and visual hierarchy
Shimmer Effect on hover that slides across elements
Enhanced Typography: Bolder fonts (800-900 weight), text shadows, gradient text effect on titles
Gradient Buttons with shimmer animations and better shadows
Better Input Fields with gradient backgrounds and enhanced focus states
Inset Highlights that give cards more dimension
Bigger Hover Transforms - cards lift further on interaction
Better Visual Depth with multiple shadow layers and stronger purple accents
Improved Spacing and padding for better visual hierarchy

Reflection:
It's at this point here that the styling has gone too far. I was just having fun with it, but ultimately the changes are not accepted.